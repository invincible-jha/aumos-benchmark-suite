# Benchmark Datasets

Dataset files are not committed to git. Place CSV/Parquet files here with the
dataset name as the filename (e.g., `adult_income.csv`, `credit_default.parquet`).

See `benchmarks/README.md` for the list of supported datasets and download instructions.
